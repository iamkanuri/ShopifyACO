# dependencies
node_modules/

# secrets — NEVER commit
.env
.env.*
!.env.example
imp keys.txt

# build output
dist/

# scan output
results/

# self-service scan runs + captured leads (contain user data / emails)
runs/
runs/leads.jsonl
leads.jsonl

# logs / os
*.log
.DS_Store
Thumbs.db
